language: node_js
cache:
  directories:
    - travis_phantomjs
before_install:
  # Upgrade PhantomJS to v2.1.1.
  - "export PHANTOMJS_VERSION=2.1.1"
  - "export PATH=$PWD/travis_phantomjs/phantomjs-$PHANTOMJS_VERSION-linux-x86_64/bin:$PATH"
  - "if [ $(phantomjs --version) != $PHANTOMJS_VERSION ]; then rm -rf $PWD/travis_phantomjs; mkdir -p $PWD/travis_phantomjs; fi"
  - "if [ $(phantomjs --version) != $PHANTOMJS_VERSION ]; then wget https://github.com/Medium/phantomjs/releases/download/v$PHANTOMJS_VERSION/phantomjs-$PHANTOMJS_VERSION-linux-x86_64.tar.bz2 -O $PWD/travis_phantomjs/phantomjs-$PHANTOMJS_VERSION-linux-x86_64.tar.bz2; fi"
  - "if [ $(phantomjs --version) != $PHANTOMJS_VERSION ]; then tar -xvf $PWD/travis_phantomjs/phantomjs-$PHANTOMJS_VERSION-linux-x86_64.tar.bz2 -C $PWD/travis_phantomjs; fi"
  - "phantomjs --version"
install : 
  - nvm install 8.11.1
  - nvm use 8.11.1
  - npm install
script:
  - node src/server.js & sleep 4 &
  - env TEST_EMAIL=$TEST_EMAIL TEST_PASSWORD=$TEST_PASSWORD API_KEY=$API_KEY TEST_PASSWORD2=$TEST_PASSWORD2 mocha test/mochaTest.js --exit
  - env TEST_EMAIL=$TEST_EMAIL TEST_PASSWORD=$TEST_PASSWORD API_KEY=$API_KEY TEST_PASSWORD2=$TEST_PASSWORD2 mocha test/CourseTest.js --exit
  # phantomjs test/testrunner.js $TEST_EMAIL $TEST_PASSWORD --ssl-protocol=any
  # casperjs test/testrunner.js $TEST_EMAIL $TEST_PASSWORD --ssl-protocol=any
after_success:
  - process.exit()
  - echo Build Succeded